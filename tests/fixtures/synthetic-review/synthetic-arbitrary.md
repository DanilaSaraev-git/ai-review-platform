# Synthetic notification flow

Events are read from an internal topic.

The consumer writes accepted events into a daily partition.

This document intentionally has no trusted expected-output binding.
